# PRESSROOM

국내 13개 언론사의 온라인 1면과 사설·오피니언을 모아 읽는 개인 뉴스 데스크.

- 로그인 전 또는 언론사 미선택 상태에는 기사 목록을 표시하지 않습니다.
- 회원별로 언론사 설정과 스크랩을 D1에 저장합니다.
- 동아일보, 조선일보, 한겨레, 경향신문, 서울신문, 세계일보, 한국경제, 매일경제, 중앙일보, 한국일보, 국민일보, 연합뉴스, 문화일보를 지원합니다.
- 온라인 1면은 홈페이지 기사 링크입니다. 종이신문의 실제 1면 PDF나 전체 기사 본문을 제공하지 않습니다.
- 새로고침 시 홈페이지와 오피니언 페이지에서 제목과 원문 URL을 수집합니다. 실패한 경로는 수집 상태에 표시하고 기존 기사를 유지합니다.
- 순위는 조회수가 아닌 공통 보도 언론사 수입니다. 저장된 기사 제목에서 핵심 단어를 추출하고 유사도를 계산해 같은 사건을 묶습니다.
- 최근 48시간 수집된 기사를 대상으로 합니다. 수집 시각과 발행 시각은 다릅니다. 발행 시각을 확인하지 못한 기사는 원문에서 확인해야 합니다.
- 이슈 팔로우업은 웹을 다시 검색하지 않고 D1에 누적된 기사를 FTS5로 검색합니다. Elasticsearch 연결 정보가 있으면 Elasticsearch를 우선 사용하고 D1을 예비 검색기로 유지합니다.

## 로컬 실행

Node 22.13 이상, npm을 사용합니다.

```sh
npm ci
npm run db:local
npm run dev
```

로컬 주소는 개발 서버가 출력합니다(기본 http://localhost:5173). 로컬 로그인은 Sites가 제공하는 테스트 사용자이며 실제 운영 인증이 아닙니다. 외부에 개발 서버를 노출하지 마세요. 운영은 Sites 인증 프록시를 통해서만 실행합니다.

## 검색 설정

D1의 articles 테이블이 원본 저장소이며 articles_fts 가상 테이블이 제목 전문 검색을 담당합니다. 검색 결과는 관련도와 기사 ID를 기준으로 결정적으로 정렬합니다.

Elastic Cloud를 연결할 때는 Worker와 수집 Worker에 ELASTICSEARCH_URL, ELASTICSEARCH_API_KEY를 secret으로 설정합니다. 선택적으로 ELASTICSEARCH_INDEX를 지정할 수 있으며 기본값은 pressroom-articles입니다. 연결 전에도 D1 FTS5 검색은 정상 작동합니다.

## 검증

```sh
npm run typecheck
npm run check:news
npm run check:news -- --live
npm run check:api
npm run build
```

check:api는 실행 중인 로컬 서버와 언론사를 선택·수집한 테스트 계정을 사용합니다. 테스트로 추가한 스크랩은 제거하고 기존 스크랩은 보존합니다. 단위 검증은 URL 중복·외부 URL 거절·언론사 수·존재하지 않는 ID·중복 주제 배치를 확인합니다.

## 운영 제한과 다음 연결 단계

2026-09-14 기존 Sites 프로젝트 연결이 복구됐습니다. 기존 ID를 유지하고 비공개 배포를 진행합니다.

2026-09-14 실제 Worker 수집에서 8개 언론사의 16개 경로 모두 성공했고 URL 중복 정리 후 272개 기사를 저장했습니다. 일시적인 시간 초과는 경로별로 표시합니다. 언론사 페이지 변경에 따라 수집기 점검이 필요합니다. 이용은 해당 언론사의 이용 조건에 따라야 하며 RSS 주소는 메타데이터일 뿐 현재 수집기는 RSS를 사용하지 않습니다.

현재 수집은 10분 간격 Cloudflare Cron Worker와 사용자의 새로고침으로 실행합니다. 검색 요청은 외부 언론사를 다시 크롤링하지 않고 저장된 데이터만 읽습니다.

DB 구조: settings(회원별 선택과 분석), articles(URL SHA-256 식별자), articles_fts(기사 제목 전문 검색), scraps(회원·기사 복합키), sources(언론사·섹션 수집 상태). Drizzle 마이그레이션은 drizzle/에 있으며 런타임에서 스키마를 생성하지 않습니다.
